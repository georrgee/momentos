<div style="background-color: darkgray; padding: 24px">
    Here is my post creation form doggy

    <form style='margin-bottom: 12px;' action="/post" method="POST">
        <input name ='postBody' type="text" placeholder="Type here">
        <button type="submit">Create Post</button>
    </form>
    
    Below are all of my posts:

    <% allPosts.forEach(p => { %>

        <div style='background-color: white; padding: 16px; margin-bottom: 12px'>
            <div style='font-weight: bold'>
                <%= p.user.fullName %>
            </div>

            <div style='color: rgb(186, 186, 186);'>
                <%= new Date(p.createdAt).toLocaleTimeString() 
                + " " + new Date(p.createdAt).toLocaleDateString() %>
            </div>
            <%= p.text %>
        </div>
    <% }) %>
</div>